{
  "name": "diversity-atlas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "diversity-atlas — ES-module flag so 'node atlases/diversity/shared/test_*.js' can load the page modules under test. There is no npm dependency graph; the browser imports atlases/diversity/**/*.js directly."
}
