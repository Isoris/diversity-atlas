# roadmap (was page8)

**Stage:** meta · **Module:** [roadmap.js](roadmap.js) · **Fragment:** [roadmap.html](roadmap.html)

## Renders

Static narrative of what's shipped vs. planned. Pure HTML fragment;
the JS module is essentially a no-op (mount/unmount hooks only).

## Reads

No data slots. Renders entirely from the fragment HTML.

## Fallback behavior

N/A — no data dependencies.
